// Override favicon based on UUID.
// Kanoka disk desktop icons for Metru chapters.

function overrideFavicon(UUID) {
    // Check if [contentpath]/favicon.ico exists.
    // If so, remove existing favicon link from page.
    // And insert replacement.
}


// If id/s != null, run overrideFavicon.